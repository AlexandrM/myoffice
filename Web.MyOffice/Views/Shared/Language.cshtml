<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@CultureHelper.GetCultureNameFromCookies(this.Request)<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var item in CultureHelper.Cultures.Keys)
        {
            <li>@Html.ActionLink(CultureHelper.Cultures[item], "SetCulture", "Home", new { culture = item }, null)</li>
        }
    </ul>
</li>
